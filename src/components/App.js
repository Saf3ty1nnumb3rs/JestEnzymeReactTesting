import React from 'react';
import { Form, FormControl, Button } from 'react-bootstrap';

const App = () => {
    return (
        <div>
            <h2>Note To Self</h2>
            <Form>
                <FormControl />
                <Button>
                    Submit
                </Button>
            </Form>
        </div>
    );
};

export default App;